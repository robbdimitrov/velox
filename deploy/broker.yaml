apiVersion: v1
kind: Service
metadata:
  name: broker
  namespace: velox
  labels:
    app.kubernetes.io/name: broker
    app.kubernetes.io/part-of: velox
spec:
  clusterIP: None
  ports:
    - name: broker
      port: 9092
      targetPort: broker
    - name: admin
      port: 9644
      targetPort: admin
  selector:
    app.kubernetes.io/name: broker
---
apiVersion: apps/v1
kind: StatefulSet
metadata:
  name: broker
  namespace: velox
  labels:
    app.kubernetes.io/name: broker
    app.kubernetes.io/part-of: velox
spec:
  serviceName: broker
  replicas: 1
  selector:
    matchLabels:
      app.kubernetes.io/name: broker
  template:
    metadata:
      labels:
        app.kubernetes.io/name: broker
        app.kubernetes.io/part-of: velox
    spec:
      containers:
        - name: broker
          image: redpandadata/redpanda:v24.2.9
          imagePullPolicy: IfNotPresent
          command:
            - /usr/bin/rpk
            - redpanda
            - start
            - --mode=dev-container
            - --overprovisioned
            - --smp=1
            - --memory=1G
            - --reserve-memory=0M
            - --node-id=0
            - --check=false
            - --kafka-addr=PLAINTEXT://0.0.0.0:9092
            - --advertise-kafka-addr=PLAINTEXT://broker.velox.svc.cluster.local:9092
          ports:
            - name: broker
              containerPort: 9092
            - name: admin
              containerPort: 9644
          readinessProbe:
            httpGet:
              path: /v1/status/ready
              port: admin
            initialDelaySeconds: 10
            periodSeconds: 10
          volumeMounts:
            - name: broker-data
              mountPath: /var/lib/redpanda/data
  volumeClaimTemplates:
    - metadata:
        name: broker-data
      spec:
        accessModes: ["ReadWriteOnce"]
        resources:
          requests:
            storage: 10Gi
